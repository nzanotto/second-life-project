function getCoordinatesFromAPI() {

    apiHandler
        .getCoordinates()
        .then(response => {
            let address = ''

        })
    console.log('esta es la direccion', address, response)
}